# 성능 검토: Python으로 바꿀 필요가 있는가?

**현재 범위에서는 JavaScript 계산을 유지하는 것이 유리합니다.** 동일한 0.25가동일 간격 알고리즘을 일반 CPython으로 이식했을 때 이번 환경에서는 약 3.3~7.2배 느렸습니다. 이것은 이번 구현의 결과이지 언어 전체의 성능 순위가 아닙니다.

## 실측

2026-09-13 UTC, Linux x86-64, Node.js 24.19.0 / CPython 3.12.14. 예열 10회·측정 50회. P95는 95백분위수입니다.

| 조건 | JS 중앙값 | Python 중앙값 | JS P95 | Python P95 |
|---|---:|---:|---:|---:|
| 기본 128기 | 0.88 ms | 2.89 ms | 1.90 ms | 3.74 ms |
| 발주 256기 | 0.78 ms | 5.62 ms | 3.14 ms | 6.76 ms |
| 장기 병목 256기 | 6.32 ms | 40.34 ms | 18.22 ms | 44.07 ms |

장기 병목: 조립 셀 1개, 인력 4명, 챔버당 1기, 조립·환경시험 각각 30가동일, 재작업 확률 30%. UI의 허용 범위 안에서 모든 발주분의 완료까지 긴 시간이 필요한 조건입니다.

각 호출은 위성별 작업·대기 이력을 모두 생성합니다. JSON 직렬화, Worker 메시지 복사, 브라우저 렌더링, 통신, 서버 기동은 제외했습니다. 캐시 적중도 측정에 포함하지 않았습니다. 입력은 `benchmarks/scenarios.json`, 실측은 `benchmarks/results.json`, 재현 명령은 `python benchmarks/compare.py`입니다.

짧은 계산은 JIT·실행 순서·스케줄링의 영향을 받습니다. 256기 중앙값이 기본값보다 일부 작은 결과는 측정 변동으로 해석해야 합니다. Node/V8 측정값은 다른 브라우저와 Render 서버의 처리량을 보장하지 않습니다.

## 적용한 개선과 검증

- Worker에서 변경 조건을 계산하고 UI는 마지막으로 완료한 결과를 표시합니다. 계산 중임을 표시합니다.
- 최신 조건 하나만 대기시킵니다. 이미 실행 중인 계산은 끝내되 오래된 결과를 화면에 반영하지 않습니다.
- 최근 24개 결과를 메모리에 저장합니다. 기준 저장·CSV는 계산 완료 후 사용할 수 있습니다.
- Worker 미지원 또는 로딩 실패 시 기본 계산 경로를 제공합니다.

기본 계산의 첫 실행은 약 1ms 수준이어서 동기로 표시합니다. Worker는 화면 반응성을 위한 변경이며 계산 그 자체를 가속했다고 주장하지 않습니다. 실기기에서의 Worker 전송 포함 체감 성능은 별도 측정이 필요합니다. [MDN Web Workers 문서](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers)

8개 시나리오에서 JS와 Python의 위성별 작업·대기 이력을 포함한 전체 결과가 일치했습니다. 수량·시간 보존, 설비 수용량, Worker 메시지 왕복, 캐시, 대기 요청 교체, 미지원·로딩 실패 경로도 검증했습니다. 브라우저 실화면과 Render 실제 배포는 검증 범위에 포함하지 않았습니다.

## Python 확장 판단

| 목적 | 권장 방향 |
|---|---|
| 현재 16~256기 시나리오 조정 | 정적 웹앱 + JS Worker |
| 로컬 설정 배열 일괄 계산·분석 | 동봉한 Python 모듈 활용 |
| 수천 회 이상 반복실험·최적화 | 이벤트 중심 알고리즘·배치 구조를 먼저 검토한 뒤 실측 |
| 공유 데이터·서버 작업 큐·협업 | Python 등 별도 API 서버를 제품 요구에 맞춰 설계 |

Python 이식 자체가 가속을 보장하지 않습니다. 대규모 확장에서는 알고리즘·자료구조·병렬화·네이티브 계산 라이브러리가 새 검토 대상입니다. 동봉 모듈은 결과 일치와 읽기 쉬운 기준 구현을 우선했으며 NumPy/Numba나 최적화된 이벤트 방식과 비교한 결과는 아닙니다.

정적 파일은 [Render Static Sites](https://render.com/docs/static-sites)가 배포하고 계산은 사용자 브라우저에서 수행합니다. 현재 규모에서 서버 계산으로 옮기면 통신과 서버 자원이 추가됩니다. 추후 API가 필요하면 [Render FastAPI 배포 방식](https://render.com/docs/deploy-fastapi)을 사용할 수 있습니다. 현재 패키지는 Static Site용 Blueprint만 생성합니다.
